import { useEffect, useMemo, useState } from 'react'
import { createRoot } from 'react-dom/client'
import {
  AlertTriangle,
  ArrowDownRight,
  ArrowUpRight,
  BarChart3,
  Bell,
  Boxes,
  Calculator,
  ChevronDown,
  ChevronRight,
  CircleDollarSign,
  ClipboardList,
  Factory,
  FileText,
  LayoutDashboard,
  Menu,
  PackageCheck,
  PanelLeftClose,
  PanelLeftOpen,
  Plus,
  Receipt,
  Search,
  Settings2,
  ShieldCheck,
  SlidersHorizontal,
  Sparkles,
  Truck,
  Users,
  X,
} from 'lucide-react'
import './styles.css'
import { apiFetch, API_URL } from './api'
import { demoInventory, demoOrders, formatBRL, inventoryLevel, mapOrders, navItems } from './domain'
import type { AppSetting, AppUser, DashboardData, InventoryItem, InvoiceItem, Partner, ProductionOrder, PurchaseInvoice, Section, StockMovement, OrderStatus } from './domain'
import { Bottleneck, CapacityChart, MetricCard, OrderRow, PageHeading, StockRow, WipView } from './components/shared'
import { OverviewScreen } from './screens/OverviewScreen'

function App() {
  const [token, setToken] = useState(() => localStorage.getItem('atelier_access_token'))
  const [section, setSection] = useState<Section>('Visão geral')
  const [sidebarOpen, setSidebarOpen] = useState(true)
  const [search, setSearch] = useState('')
  const [selectedLine, setSelectedLine] = useState('Todas as linhas')
  const [showNotifications, setShowNotifications] = useState(false)
  const [fiscalMenuOpen, setFiscalMenuOpen] = useState(true)
  const [financeMenuOpen, setFinanceMenuOpen] = useState(true)
  const [usersMenuOpen, setUsersMenuOpen] = useState(true)
  const [currentUser, setCurrentUser] = useState<AppUser | null>(null)
  const [cost, setCost] = useState(148.5)
  const [margin, setMargin] = useState(32)
  const [tax, setTax] = useState(12.45)
  const [ordersData, setOrdersData] = useState<ProductionOrder[]>(demoOrders)
  const [inventoryData, setInventoryData] = useState<InventoryItem[]>(demoInventory)
  const [invoices, setInvoices] = useState<PurchaseInvoice[]>([])
  const [loadingData, setLoadingData] = useState(false)
  const [dataError, setDataError] = useState('')

  useEffect(() => {
    if (!token) return
    apiFetch<AppUser>('/auth/me', token).then(setCurrentUser).catch(() => setCurrentUser(null))
    setLoadingData(true)
    apiFetch<DashboardData>('/dashboard', token)
      .then((dashboard) => { setOrdersData(mapOrders(dashboard.orders)); setInventoryData(dashboard.inventory) })
      .catch((error: unknown) => setDataError(error instanceof Error ? error.message : 'Falha ao carregar dados'))
      .finally(() => setLoadingData(false))
  }, [token])

  const filteredOrders = useMemo(() => {
    const term = search.toLowerCase().trim()
    if (!term) return ordersData
    return ordersData.filter((order) => `${order.id} ${order.product} ${order.station}`.toLowerCase().includes(term))
  }, [ordersData, search])

  const salePrice = cost / (1 - (margin + tax) / 100)
  const contribution = salePrice - cost - salePrice * (tax / 100)
  const canSee = (permission: string) => currentUser?.role === 'admin' || Boolean(currentUser?.permissions?.includes(permission))
  const sectionPermission: Record<string, string> = { 'Visão geral': 'dashboard', Produção: 'production', Estoque: 'inventory', Movimentações: 'movements', Fornecedores: 'partners', Clientes: 'partners', 'Contas a pagar': 'finance', Configurações: 'settings' }

  if (!token) {
    return <LoginScreen onLogin={(accessToken) => { localStorage.setItem('atelier_access_token', accessToken); setToken(accessToken) }} />
  }

  return (
    <div className="app-shell">
      <aside className={`sidebar ${sidebarOpen ? 'is-open' : 'is-collapsed'}`}>
        <div className="brand-lockup">
          <div className="brand-mark"><Sparkles size={18} strokeWidth={2.5} /></div>
          {sidebarOpen && <div><strong>ATELIER</strong><span>INDUSTRIAL OS</span></div>}
        </div>
        <div className="workspace-switcher">
          <div className="workspace-avatar">AM</div>
          {sidebarOpen && <><div className="workspace-copy"><strong>Ateliê Móveis</strong><span>Unidade principal</span></div><ChevronRight size={15} /></>}
        </div>
        <nav className="main-nav" aria-label="Navegação principal">
          {navItems.filter(({ label }) => canSee(sectionPermission[label])).map(({ label, icon: Icon }) => (
            <button key={label} className={`nav-item ${section === label ? 'active' : ''}`} onClick={() => setSection(label)} title={label}>
              <Icon size={18} />{sidebarOpen && <span>{label}</span>}{sidebarOpen && label === 'Estoque' && <span className="nav-badge">3</span>}
            </button>
          ))}
          {canSee('purchasing') && <><button className={`nav-item ${(section === 'Nota fiscal de entrada' || section === 'Nota fiscal de venda') ? 'active' : ''}`} onClick={() => setFiscalMenuOpen(!fiscalMenuOpen)} title="Notas fiscais"><Receipt size={18} />{sidebarOpen && <span>Notas fiscais</span>}{sidebarOpen && (fiscalMenuOpen ? <ChevronDown size={14} /> : <ChevronRight size={14} />)}</button>{sidebarOpen && fiscalMenuOpen && <div className="nav-submenu"><button className={`nav-subitem ${section === 'Nota fiscal de entrada' ? 'active' : ''}`} onClick={() => setSection('Nota fiscal de entrada')}>NF de entrada</button><button className="nav-subitem wip" onClick={() => setSection('Nota fiscal de venda')}>NF de venda <small>WIP</small></button></div>}</>}
          {canSee('finance') && <><button className={`nav-item ${section === 'Contas a pagar' ? 'active' : ''}`} onClick={() => setFinanceMenuOpen(!financeMenuOpen)} title="Financeiro"><CircleDollarSign size={18} />{sidebarOpen && <span>Financeiro</span>}{sidebarOpen && (financeMenuOpen ? <ChevronDown size={14} /> : <ChevronRight size={14} />)}</button>{sidebarOpen && financeMenuOpen && <div className="nav-submenu"><button className={`nav-subitem ${section === 'Contas a pagar' ? 'active' : ''}`} onClick={() => setSection('Contas a pagar')}>Contas a pagar</button></div>}</>}
        </nav>
        {sidebarOpen && <div className="nav-label">Gestão</div>}
        <nav className="secondary-nav">
          {canSee('movements') && <button className={`nav-item ${section === 'Movimentações' ? 'active' : ''}`} onClick={() => setSection('Movimentações')}><PackageCheck size={18} />{sidebarOpen && <span>Movimentações</span>}</button>}
          <button className="nav-item" onClick={() => setSection('Relatórios' as Section)}><BarChart3 size={18} />{sidebarOpen && <span>Relatórios</span>}{sidebarOpen && <small className="nav-wip">WIP</small>}</button>
        </nav>
        <div className="sidebar-bottom">
          {canSee('settings') && <button className={`nav-item ${section === 'Configurações' ? 'active' : ''}`} onClick={() => setSection('Configurações')}><Settings2 size={18} />{sidebarOpen && <span>Configurações</span>}</button>}
          {currentUser?.role === 'admin' && <><button className={`nav-item ${(section === 'Usuários' || section === 'Funcionários') ? 'active' : ''}`} onClick={() => setUsersMenuOpen(!usersMenuOpen)}><Users size={18} />{sidebarOpen && <span>Usuários</span>}{sidebarOpen && (usersMenuOpen ? <ChevronDown size={14} /> : <ChevronRight size={14} />)}</button>{sidebarOpen && usersMenuOpen && <div className="nav-submenu"><button className={`nav-subitem ${section === 'Usuários' ? 'active' : ''}`} onClick={() => setSection('Usuários')}>Usuários</button><button className="nav-subitem wip" onClick={() => setSection('Funcionários')}>Funcionários <small>WIP</small></button></div>}</>}
          <div className="profile-row"><div className="profile-avatar">RS</div>{sidebarOpen && <div className="workspace-copy"><strong>Rafael Silva</strong><span>Administrador</span></div>}</div>
        </div>
      </aside>

      <main className="main-content">
        <header className="topbar">
          <div className="topbar-left"><button className="icon-button menu-button" onClick={() => setSidebarOpen(!sidebarOpen)} aria-label="Alternar menu">{sidebarOpen ? <PanelLeftClose size={18} /> : <PanelLeftOpen size={18} />}</button><span className="breadcrumb">Operação <ChevronRight size={14} /> <strong>{section}</strong></span></div>
          <div className="topbar-actions"><div className="status-pill"><span className="status-dot" /> API conectada</div><button className="icon-button notification-button" onClick={() => setShowNotifications(!showNotifications)} aria-label="Notificações"><Bell size={18} /><span /></button><button className="logout-button" onClick={() => { localStorage.removeItem('atelier_access_token'); setToken(null) }}>Sair</button><div className="topbar-date">09 setembro 2026</div></div>
          {showNotifications && <div className="notification-popover"><div className="popover-heading"><strong>Notificações</strong><span>3 novas</span></div><div className="notification-item"><AlertTriangle size={16} /><div><strong>Verniz PU abaixo do mínimo</strong><small>Estoque · há 12 min</small></div></div><div className="notification-item"><Truck size={16} /><div><strong>Recebimento NF 02841 conferido</strong><small>Almoxarifado · há 42 min</small></div></div><div className="notification-item"><ShieldCheck size={16} /><div><strong>OP-2406 encerrada com sucesso</strong><small>Produção · há 1 h</small></div></div></div>}
        </header>

        <div className="page-wrap">
          {dataError && <div className="api-error"><AlertTriangle size={15} /> {dataError}</div>}
          {loadingData && <div className="data-loading">Sincronizando dados da operação...</div>}
          {section === 'Visão geral' && <OverviewScreen orders={ordersData} inventory={inventoryData} onOpenProduction={() => setSection('Produção')} onOpenStock={() => setSection('Estoque')} selectedLine={selectedLine} setSelectedLine={setSelectedLine} />}
          {section === 'Produção' && <ProductionViewLive token={token} search={search} setSearch={setSearch} filteredOrders={filteredOrders} onCreated={(order) => setOrdersData((current) => [order, ...current])} />}
          {section === 'Estoque' && <StockViewCrud token={token} inventory={inventoryData} onCreated={(item) => setInventoryData((current) => [...current, item])} onUpdated={(item) => setInventoryData((current) => current.map((currentItem) => currentItem.id === item.id ? item : currentItem))} onDeleted={(id) => setInventoryData((current) => current.filter((item) => item.id !== id))} />}
          {section === 'Movimentações' && <MovementsView token={token} inventory={inventoryData} onChanged={() => apiFetch<InventoryItem[]>('/inventory', token).then(setInventoryData)} />}
          {section === 'Nota fiscal de entrada' && <InvoiceViewWithPayable token={token} inventory={inventoryData} invoices={invoices} onLoaded={setInvoices} onReceived={(invoice) => { setInvoices((current) => current.map((currentInvoice) => currentInvoice.id === invoice.id ? invoice : currentInvoice)); apiFetch<InventoryItem[]>('/inventory', token).then(setInventoryData) }} />}
          {section === 'Nota fiscal de venda' && <WipView title="Nota fiscal de venda" description="Este módulo está reservado para a próxima etapa comercial." />}
          {section === 'Fornecedores' && <PartnerView token={token} kind="suppliers" />}
          {section === 'Clientes' && <PartnerView token={token} kind="customers" />}
          {section === 'Contas a pagar' && <PayablesView token={token} />}
          {section === 'Configurações' && <SettingsView token={token} />}
          {section === 'Usuários' && <UsersView token={token} />}
          {section === 'Funcionários' && <WipView title="Funcionários" description="O cadastro de funcionários será implementado na próxima etapa." />}
          {section === ('Relatórios' as Section) && <WipView title="Relatórios" description="Os relatórios gerenciais serão liberados na próxima etapa." />}
        </div>
      </main>
    </div>
  )
}

function LoginScreen({ onLogin }: { onLogin: (token: string) => void }) {
  const [email, setEmail] = useState('admin@atelier.com')
  const [password, setPassword] = useState('atelier123')
  const [error, setError] = useState('')
  const [loading, setLoading] = useState(false)

  async function submit(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault()
    setError('')
    setLoading(true)
    try {
      const response = await fetch(`${API_URL}/auth/login`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email, password }) })
      if (!response.ok) throw new Error('E-mail ou senha inválidos')
      const result = await response.json() as { access_token: string }
      onLogin(result.access_token)
    } catch (requestError) {
      setError(requestError instanceof Error ? requestError.message : 'Não foi possível conectar à API')
    } finally {
      setLoading(false)
    }
  }

  return <div className="login-shell"><div className="login-art"><div className="brand-lockup"><div className="brand-mark"><Sparkles size={18} strokeWidth={2.5} /></div><div><strong>ATELIER</strong><span>INDUSTRIAL OS</span></div></div><div className="login-art-copy"><span className="eyebrow">Controle que acompanha o ritmo</span><h1>Da madeira ao móvel pronto.</h1><p>Uma visão única para produção, estoque e margem da sua fábrica.</p></div><div className="login-art-footer"><span>PCP · CUSTOS · ESTOQUE</span><span>v0.1 MVP</span></div></div><div className="login-panel"><div className="login-panel-inner"><div className="mobile-brand"><div className="brand-mark"><Sparkles size={18} /></div><strong>ATELIER</strong></div><span className="eyebrow">Acesso ao workspace</span><h2>Bem-vindo de volta</h2><p>Entre para acompanhar a operação da Ateliê Móveis.</p><form onSubmit={submit}><label>E-mail<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} autoComplete="email" required /></label><label>Senha<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} autoComplete="current-password" required /></label>{error && <div className="login-error"><AlertTriangle size={15} />{error}</div>}<button className="primary-button login-button" disabled={loading}>{loading ? 'Conectando...' : 'Entrar no sistema'}<ChevronRight size={16} /></button></form><small className="login-hint">Demo MVP: admin@atelier.com · atelier123</small></div></div></div>
}

function ProductionViewLive({ token, search, setSearch, filteredOrders, onCreated }: { token: string; search: string; setSearch: (value: string) => void; filteredOrders: ProductionOrder[]; onCreated: (order: ProductionOrder) => void }) {
  const [showForm, setShowForm] = useState(false)
  const [saving, setSaving] = useState(false)
  const [error, setError] = useState('')
  const [form, setForm] = useState({ code: '', product: '', quantity: '1', station: 'Corte', due: '' })
  const inProgress = filteredOrders.filter((order) => order.status === 'Em produção').length
  const waiting = filteredOrders.filter((order) => order.status === 'Aguardando').length
  const completed = filteredOrders.filter((order) => order.status === 'Concluída').length
  const pieces = filteredOrders.reduce((total, order) => total + order.quantity, 0)

  async function createOrder(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault(); setSaving(true); setError('')
    try {
      const created = await apiFetch<{ code: string; product: string; quantity: number; progress: number; station: string; due: string; status: OrderStatus }>('/production-orders', token, { method: 'POST', body: JSON.stringify({ ...form, quantity: Number(form.quantity) }) })
      onCreated({ id: created.code, product: created.product, quantity: created.quantity, progress: created.progress, station: created.station, due: created.due, status: created.status })
      setForm({ code: '', product: '', quantity: '1', station: 'Corte', due: '' }); setShowForm(false)
    } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao criar OP') } finally { setSaving(false) }
  }

  return <><PageHeading eyebrow="PCP · Fábrica" title="Ordens de produção" description="Acompanhe o avanço de cada ordem e mantenha o fluxo sem interrupções." action={<button className="primary-button" onClick={() => setShowForm(true)}><Plus size={17} /> Emitir OP</button>} /><div className="production-summary"><div><span>Em andamento</span><strong>{String(inProgress).padStart(2, '0')}</strong></div><div><span>Em espera</span><strong>{String(waiting).padStart(2, '0')}</strong></div><div><span>Concluídas</span><strong>{String(completed).padStart(2, '0')}</strong></div><div><span>Peças no fluxo</span><strong>{pieces}</strong></div></div>{showForm && <section className="panel create-panel"><div className="panel-header"><div><span className="panel-kicker">Nova ordem</span><h2>Emitir ordem de produção</h2></div><button className="icon-button" onClick={() => setShowForm(false)} aria-label="Fechar"><X size={16} /></button></div><form className="create-form" onSubmit={createOrder}><label>Código da OP<input required value={form.code} onChange={(event) => setForm({ ...form, code: event.target.value })} placeholder="OP-2409" /></label><label>Produto<input required value={form.product} onChange={(event) => setForm({ ...form, product: event.target.value })} placeholder="Cadeira Lina · Natural" /></label><label>Quantidade<input required min="1" type="number" value={form.quantity} onChange={(event) => setForm({ ...form, quantity: event.target.value })} /></label><label>Primeira etapa<select value={form.station} onChange={(event) => setForm({ ...form, station: event.target.value })}><option>Corte</option><option>Usinagem</option><option>Montagem</option><option>Acabamento</option><option>Estofamento</option></select></label><label>Prazo<input required value={form.due} onChange={(event) => setForm({ ...form, due: event.target.value })} placeholder="13 set, 08:00" /></label>{error && <div className="login-error"><AlertTriangle size={15} />{error}</div>}<button className="primary-button" disabled={saving}>{saving ? 'Emitindo...' : 'Emitir OP'}</button></form></section>}<section className="panel full-panel"><div className="panel-toolbar"><div className="search-box"><Search size={16} /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Buscar por OP, produto ou etapa" /></div><button className="secondary-button"><SlidersHorizontal size={16} /> Filtrar</button><button className="secondary-button"><FileText size={16} /> Exportar</button></div><div className="full-order-table"><div className="table-head"><span>Ordem / produto</span><span>Etapa atual</span><span>Avanço</span><span>Prazo</span><span>Status</span></div>{filteredOrders.map((order) => <div className="full-order-row" key={order.id}><div className="order-id"><span className="status-marker" data-status={order.status} /><div><strong>{order.id}</strong><small>{order.product} · lote de {order.quantity} un</small></div></div><span className="stage-label">{order.station}</span><div className="progress-wrap"><div className="progress-track"><span style={{ width: `${order.progress}%` }} /></div><small>{order.progress}%</small></div><span>{order.due}</span><span className={`pill-status ${order.status.toLowerCase().replace(' ', '-')}`}>{order.status}</span></div>)}</div></section></>
}

function StockViewCrud({ token, inventory, onCreated, onUpdated, onDeleted }: { token: string; inventory: InventoryItem[]; onCreated: (item: InventoryItem) => void; onUpdated: (item: InventoryItem) => void; onDeleted: (id: number) => void }) {
  const [showForm, setShowForm] = useState(false)
  const [editing, setEditing] = useState<InventoryItem | null>(null)
  const [saving, setSaving] = useState(false)
  const [error, setError] = useState('')
  const [form, setForm] = useState({ sku: '', name: '', stock: '0', unit: 'un', minimum_stock: '0' })
  const belowMinimum = inventory.filter((item) => item.stock <= item.minimum_stock).length

  function openCreate() { setEditing(null); setForm({ sku: '', name: '', stock: '0', unit: 'un', minimum_stock: '0' }); setError(''); setShowForm(true) }
  function openEdit(item: InventoryItem) { setEditing(item); setForm({ sku: item.sku, name: item.name, stock: String(item.stock), unit: item.unit, minimum_stock: String(item.minimum_stock) }); setError(''); setShowForm(true) }
  async function saveItem(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault(); setSaving(true); setError('')
    try {
      const path = editing ? `/inventory/${editing.id}` : '/inventory'
      const body = editing ? { name: form.name, unit: form.unit, minimum_stock: Number(form.minimum_stock) } : { ...form, stock: Number(form.stock), minimum_stock: Number(form.minimum_stock) }
      const item = await apiFetch<InventoryItem>(path, token, { method: editing ? 'PUT' : 'POST', body: JSON.stringify(body) })
      if (editing) onUpdated(item); else onCreated(item)
      setShowForm(false)
    } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao salvar material') } finally { setSaving(false) }
  }
  async function removeItem(item: InventoryItem) {
    if (!window.confirm(`Excluir ${item.sku} - ${item.name}?`)) return
    try { await apiFetch<unknown>(`/inventory/${item.id}`, token, { method: 'DELETE' }); onDeleted(item.id) } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao excluir material') }
  }
  return <><PageHeading eyebrow="Almoxarifado · Inventário" title="Estoque e materiais" description="Cadastre, edite e acompanhe os materiais que alimentam a produção." action={<button className="primary-button" onClick={openCreate}><Plus size={17} /> Cadastrar material</button>} /><div className="metric-grid stock-metrics"><MetricCard label="Itens cadastrados" value={String(inventory.length)} detail="dados sincronizados" trend="API" direction="up" accent="teal" /><MetricCard label="Abaixo do mínimo" value={String(belowMinimum).padStart(2, '0')} detail="reposição necessária" trend={String(belowMinimum)} direction="down" accent="peach" /><MetricCard label="Última entrada" value="Hoje" detail="via módulo de NFs" trend="API" direction="up" accent="cream" /><MetricCard label="Giro médio" value="3,8x" detail="últimos 90 dias" trend="0,4x" direction="up" accent="lilac" /></div>{showForm && <section className="panel create-panel"><div className="panel-header"><div><span className="panel-kicker">{editing ? 'Editar material' : 'Novo material'}</span><h2>{editing ? editing.sku : 'Cadastrar item de estoque'}</h2></div><button className="icon-button" onClick={() => setShowForm(false)} aria-label="Fechar"><X size={16} /></button></div><form className="create-form" onSubmit={saveItem}><label>SKU<input required disabled={Boolean(editing)} value={form.sku} onChange={(event) => setForm({ ...form, sku: event.target.value })} placeholder="MAT-040" /></label><label>Descrição<input required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} placeholder="Cola PVA extra forte" /></label><label>Quantidade{editing ? <input disabled value={form.stock} /> : <input required min="0" type="number" step="0.01" value={form.stock} onChange={(event) => setForm({ ...form, stock: event.target.value })} />}</label><label>Unidade<select value={form.unit} onChange={(event) => setForm({ ...form, unit: event.target.value })}><option>un</option><option>m</option><option>m³</option><option>L</option><option>kg</option></select></label><label>Estoque mínimo<input required min="0" type="number" step="0.01" value={form.minimum_stock} onChange={(event) => setForm({ ...form, minimum_stock: event.target.value })} /></label>{error && <div className="login-error"><AlertTriangle size={15} />{error}</div>}<button className="primary-button" disabled={saving}>{saving ? 'Salvando...' : editing ? 'Salvar alterações' : 'Salvar material'}</button></form></section>}<section className="panel full-panel"><div className="panel-header"><div><span className="panel-kicker">Inventário atual</span><h2>Materiais sincronizados</h2></div><button className="text-button"><ShieldCheck size={15} /> Dados reais</button></div>{error && !showForm && <div className="api-error" style={{ margin: '0 22px 14px' }}><AlertTriangle size={15} />{error}</div>}<div className="inventory-grid">{inventory.map((item) => <div className="inventory-card" key={item.id}><div className={`inventory-card-icon ${item.color}`}><Boxes size={19} /></div><div className="inventory-card-top"><span>{item.sku}</span><span className={`inventory-state ${item.color}`}>{item.state}</span></div><h3>{item.name}</h3><strong>{item.stock.toLocaleString('pt-BR')} {item.unit}</strong><div className="inventory-meter"><span className={item.color} style={{ width: `${inventoryLevel(item)}%` }} /></div><div className="inventory-footer"><small>Entrada: {item.last_entry}</small><div className="card-actions"><button className="mini-action" onClick={() => openEdit(item)}>Editar</button><button className="mini-action danger" onClick={() => removeItem(item)}>Excluir</button></div></div></div>)}</div></section></>
}

function InvoiceView({ token, inventory, invoices, onLoaded, onReceived }: { token: string; inventory: InventoryItem[]; invoices: PurchaseInvoice[]; onLoaded: (invoices: PurchaseInvoice[]) => void; onReceived: (invoice: PurchaseInvoice) => void }) {
  const [showForm, setShowForm] = useState(false)
  const [saving, setSaving] = useState(false)
  const [receiving, setReceiving] = useState<number | null>(null)
  const [error, setError] = useState('')
  const [supplierQuery, setSupplierQuery] = useState('')
  const [supplierSuggestions, setSupplierSuggestions] = useState<Partner[]>([])
  const [selectedSupplier, setSelectedSupplier] = useState<Partner | null>(null)
  const [form, setForm] = useState({ number: '', supplier: '', issue_date: '09/09/2026', sku: inventory[0]?.sku ?? '', quantity: '1', unit_cost: '0' })
  const [items, setItems] = useState([{ sku: inventory[0]?.sku ?? '', quantity: '1', unit_cost: '0' }])
  useEffect(() => { apiFetch<PurchaseInvoice[]>('/invoices', token).then(onLoaded).catch((requestError: unknown) => setError(requestError instanceof Error ? requestError.message : 'Erro ao carregar NFs')) }, [token, onLoaded])
  useEffect(() => {
    if (supplierQuery.trim().length < 2) { setSupplierSuggestions([]); return }
    const timer = window.setTimeout(() => { apiFetch<Partner[]>(`/suppliers?q=${encodeURIComponent(supplierQuery)}`, token).then(setSupplierSuggestions).catch(() => setSupplierSuggestions([])) }, 220)
    return () => window.clearTimeout(timer)
  }, [supplierQuery, token])
      async function createInvoice(event: React.FormEvent<HTMLFormElement>) {
        event.preventDefault(); setSaving(true); setError('')
        try { const invoice = await apiFetch<PurchaseInvoice>('/invoices', token, { method: 'POST', body: JSON.stringify({ number: form.number, supplier: form.supplier, supplier_id: selectedSupplier?.id ?? null, issue_date: form.issue_date, items: items.map((item) => ({ ...item, quantity: Number(item.quantity), unit_cost: Number(item.unit_cost), description: inventory.find((stockItem) => stockItem.sku === item.sku)?.name ?? item.sku })) }) }); onLoaded([invoice, ...invoices]); setShowForm(false); setForm({ number: '', supplier: '', issue_date: '09/09/2026', sku: inventory[0]?.sku ?? '', quantity: '1', unit_cost: '0' }); setItems([{ sku: inventory[0]?.sku ?? '', quantity: '1', unit_cost: '0' }]); setSupplierQuery(''); setSelectedSupplier(null) } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao cadastrar NF') } finally { setSaving(false) }
  }
  async function receive(invoice: PurchaseInvoice) { setReceiving(invoice.id); setError(''); try { const received = await apiFetch<PurchaseInvoice>(`/invoices/${invoice.id}/receive`, token, { method: 'POST' }); onReceived(received) } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao dar entrada') } finally { setReceiving(null) } }
  return <><PageHeading eyebrow="Fiscal · Almoxarifado" title="Notas fiscais de entrada" description="Registre documentos de fornecedores e dê entrada nos materiais com rastreabilidade." action={<button className="primary-button" onClick={() => setShowForm(true)}><Plus size={17} /> Nova NF de entrada</button>} />{error && <div className="api-error"><AlertTriangle size={15} />{error}</div>}{showForm && <section className="panel create-panel"><div className="panel-header"><div><span className="panel-kicker">Recebimento fiscal</span><h2>Registrar nota fiscal</h2></div><button className="icon-button" onClick={() => setShowForm(false)} aria-label="Fechar"><X size={16} /></button></div><form className="create-form" onSubmit={createInvoice}><label>Número da NF<input required value={form.number} onChange={(event) => setForm({ ...form, number: event.target.value })} placeholder="02842" /></label><label>Fornecedor<div className="autocomplete"><input required value={form.supplier} onChange={(event) => { setForm({ ...form, supplier: event.target.value }); setSupplierQuery(event.target.value); setSelectedSupplier(null) }} placeholder="Digite nome, fantasia ou CNPJ" />{supplierSuggestions.length > 0 && <div className="suggestion-list">{supplierSuggestions.map((supplier) => <button type="button" key={supplier.id} onClick={() => { setForm({ ...form, supplier: supplier.trade_name }); setSupplierQuery(supplier.trade_name); setSelectedSupplier(supplier); setSupplierSuggestions([]) }}><strong>{supplier.trade_name}</strong><small>{supplier.legal_name} · {supplier.document}</small></button>)}</div>}</div></label><label>Data de emissão<input required value={form.issue_date} onChange={(event) => setForm({ ...form, issue_date: event.target.value })} /></label><label>Material<select required value={form.sku} onChange={(event) => setForm({ ...form, sku: event.target.value })}>{inventory.map((item) => <option key={item.sku} value={item.sku}>{item.sku} · {item.name}</option>)}</select></label><label>Quantidade<input required min="0.01" type="number" step="0.01" value={form.quantity} onChange={(event) => setForm({ ...form, quantity: event.target.value })} /></label><label>Custo unitário<input required min="0.01" type="number" step="0.01" value={form.unit_cost} onChange={(event) => setForm({ ...form, unit_cost: event.target.value })} /></label><button className="primary-button" disabled={saving}>{saving ? 'Registrando...' : 'Salvar NF pendente'}</button></form></section>}<section className="panel full-panel"><div className="panel-header"><div><span className="panel-kicker">Documentos recentes</span><h2>Entradas fiscais</h2></div><span className="draft-status">{invoices.length} documentos</span></div><div className="invoice-list">{invoices.length === 0 ? <div className="empty-state"><Receipt size={22} /><strong>Nenhuma NF registrada</strong><span>Cadastre a primeira nota para iniciar uma entrada de estoque.</span></div> : invoices.map((invoice) => <div className="invoice-row" key={invoice.id}><div className="invoice-icon"><Receipt size={17} /></div><div className="invoice-main"><strong>NF {invoice.number}</strong><small>{invoice.supplier} · {invoice.issue_date}</small></div><div><strong>{formatBRL(invoice.total)}</strong><small>{invoice.items.length} item(ns)</small></div><span className={`pill-status ${invoice.status === 'Recebida' ? 'concluída' : 'aguardando'}`}>{invoice.status}</span>{invoice.status === 'Pendente' && <button className="secondary-button" disabled={receiving === invoice.id} onClick={() => receive(invoice)}>{receiving === invoice.id ? 'Processando...' : 'Dar entrada'}</button>}</div>)}</div></section></>
}

function InvoiceViewWithPayable({ token, inventory, invoices, onLoaded, onReceived }: { token: string; inventory: InventoryItem[]; invoices: PurchaseInvoice[]; onLoaded: (invoices: PurchaseInvoice[]) => void; onReceived: (invoice: PurchaseInvoice) => void }) {
  const [showForm, setShowForm] = useState(false)
  const [saving, setSaving] = useState(false)
  const [receiving, setReceiving] = useState<number | null>(null)
  const [error, setError] = useState('')
  const [supplier, setSupplier] = useState<Partner | null>(null)
  const [supplierQuery, setSupplierQuery] = useState('')
  const [suggestions, setSuggestions] = useState<Partner[]>([])
  const [form, setForm] = useState({ number: '', supplier: '', issue_date: '09/09/2026', due_date: '', description: 'Compra de insumos', payment_method: 'Boleto', category: 'Insumos', barcode: '', notes: '' })
  const [items, setItems] = useState([{ sku: inventory[0]?.sku ?? '', quantity: '1', unit_cost: '0' }])
  useEffect(() => { apiFetch<PurchaseInvoice[]>('/invoices', token).then(onLoaded).catch((requestError: unknown) => setError(requestError instanceof Error ? requestError.message : 'Erro ao carregar NFs')) }, [token, onLoaded])
  useEffect(() => { if (supplierQuery.trim().length < 2) { setSuggestions([]); return }; const timer = window.setTimeout(() => apiFetch<Partner[]>(`/suppliers?q=${encodeURIComponent(supplierQuery)}`, token).then(setSuggestions).catch(() => setSuggestions([])), 220); return () => window.clearTimeout(timer) }, [supplierQuery, token])
  function updateItem(index: number, key: 'sku' | 'quantity' | 'unit_cost', value: string) { setItems((current) => current.map((item, itemIndex) => itemIndex === index ? { ...item, [key]: value } : item)) }
  async function submit(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault(); setSaving(true); setError('')
    try {
      const payload = { ...form, supplier_id: supplier?.id ?? null, items: items.map((item) => ({ ...item, quantity: Number(item.quantity), unit_cost: Number(item.unit_cost), description: inventory.find((stockItem) => stockItem.sku === item.sku)?.name ?? item.sku })) }
      const invoice = await apiFetch<PurchaseInvoice>('/invoices', token, { method: 'POST', body: JSON.stringify(payload) })
      onLoaded([invoice, ...invoices]); setShowForm(false); setSupplier(null); setSupplierQuery(''); setItems([{ sku: inventory[0]?.sku ?? '', quantity: '1', unit_cost: '0' }])
    } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao cadastrar NF') } finally { setSaving(false) }
  }
  async function receive(invoice: PurchaseInvoice) { setReceiving(invoice.id); setError(''); try { onReceived(await apiFetch<PurchaseInvoice>(`/invoices/${invoice.id}/receive`, token, { method: 'POST' })) } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao dar entrada e criar conta a pagar') } finally { setReceiving(null) } }
  return <><PageHeading eyebrow="Fiscal · Almoxarifado" title="Notas fiscais de entrada" description="A confirmação da NF atualiza o estoque e cria automaticamente uma conta a pagar." action={<button className="primary-button" onClick={() => setShowForm(true)}><Plus size={17} /> Nova NF de entrada</button>} />{error && <div className="api-error"><AlertTriangle size={15} />{error}</div>}{showForm && <section className="panel create-panel"><div className="panel-header"><div><span className="panel-kicker">Recebimento fiscal</span><h2>Registrar nota fiscal</h2></div><button className="icon-button" onClick={() => setShowForm(false)} aria-label="Fechar"><X size={16} /></button></div><form className="create-form invoice-form" onSubmit={submit}><label>Número da NF<input required value={form.number} onChange={(event) => setForm({ ...form, number: event.target.value })} /></label><label>Fornecedor<div className="autocomplete"><input required value={form.supplier} onChange={(event) => { setForm({ ...form, supplier: event.target.value }); setSupplierQuery(event.target.value); setSupplier(null) }} placeholder="Digite nome, fantasia ou CNPJ" />{suggestions.length > 0 && <div className="suggestion-list">{suggestions.map((item) => <button type="button" key={item.id} onClick={() => { setForm({ ...form, supplier: item.trade_name }); setSupplier(item); setSupplierQuery(item.trade_name); setSuggestions([]) }}><strong>{item.trade_name}</strong><small>{item.legal_name} · {item.document}</small></button>)}</div>}</div></label><label>Data de emissão<input required value={form.issue_date} onChange={(event) => setForm({ ...form, issue_date: event.target.value })} /></label><label>Data de vencimento<input required value={form.due_date} onChange={(event) => setForm({ ...form, due_date: event.target.value })} placeholder="09/10/2026" /></label><label>Descrição<input required value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} /></label><label>Forma de pagamento<select value={form.payment_method} onChange={(event) => setForm({ ...form, payment_method: event.target.value })}><option>Boleto</option><option>PIX</option><option>Transferência</option><option>Cartão</option><option>Dinheiro</option></select></label><label>Categoria<select value={form.category} onChange={(event) => setForm({ ...form, category: event.target.value })}><option>Insumos</option><option>Fretes</option><option>Serviços</option><option>Impostos</option><option>Outros</option></select></label><label>Código de barras<input value={form.barcode} onChange={(event) => setForm({ ...form, barcode: event.target.value })} /></label><label>Observações<input value={form.notes} onChange={(event) => setForm({ ...form, notes: event.target.value })} /></label><div className="invoice-items-field"><div className="invoice-items-heading"><strong>Itens da nota ({items.length})</strong><button type="button" className="secondary-button" onClick={() => setItems([...items, { sku: inventory[0]?.sku ?? '', quantity: '1', unit_cost: '0' }])}><Plus size={14} /> Adicionar item</button></div>{items.map((item, index) => <div className="invoice-item-editor" key={index}><select required value={item.sku} onChange={(event) => updateItem(index, 'sku', event.target.value)}>{inventory.map((stockItem) => <option key={stockItem.sku} value={stockItem.sku}>{stockItem.sku} · {stockItem.name}</option>)}</select><input required min="0.01" type="number" step="0.01" value={item.quantity} onChange={(event) => updateItem(index, 'quantity', event.target.value)} placeholder="Quantidade" /><input required min="0.01" type="number" step="0.01" value={item.unit_cost} onChange={(event) => updateItem(index, 'unit_cost', event.target.value)} placeholder="Custo unitário" />{items.length > 1 && <button type="button" className="icon-button" onClick={() => setItems(items.filter((_, itemIndex) => itemIndex !== index))} aria-label="Remover item"><X size={15} /></button>}</div>)}</div><button className="primary-button" disabled={saving}>{saving ? 'Registrando...' : 'Salvar NF pendente'}</button></form></section>}<section className="panel full-panel"><div className="panel-header"><div><span className="panel-kicker">Documentos recentes</span><h2>Entradas fiscais</h2></div><span className="draft-status">{invoices.length} documentos</span></div><div className="invoice-list">{invoices.length === 0 ? <div className="empty-state"><Receipt size={22} /><strong>Nenhuma NF registrada</strong><span>Cadastre a primeira nota para iniciar uma entrada de estoque.</span></div> : invoices.map((invoice) => <div className="invoice-row" key={invoice.id}><div className="invoice-icon"><Receipt size={17} /></div><div className="invoice-main"><strong>NF {invoice.number}</strong><small>{invoice.supplier} · {invoice.issue_date}</small></div><div><strong>{formatBRL(Number(invoice.total))}</strong><small>Vence {invoice.due_date || invoice.issue_date}</small></div><span className={`pill-status ${invoice.status === 'Recebida' ? 'concluída' : 'aguardando'}`}>{invoice.status}</span>{invoice.status === 'Pendente' && <button className="secondary-button" disabled={receiving === invoice.id} onClick={() => receive(invoice)}>{receiving === invoice.id ? 'Processando...' : 'Dar entrada + pagar'}</button>}</div>)}</div></section></>
}

function InvoiceViewMulti({ token, inventory, invoices, onLoaded, onReceived }: { token: string; inventory: InventoryItem[]; invoices: PurchaseInvoice[]; onLoaded: (invoices: PurchaseInvoice[]) => void; onReceived: (invoice: PurchaseInvoice) => void }) {
  const emptyItem = { sku: inventory[0]?.sku ?? '', quantity: '1', unit_cost: '0' }
  const [showForm, setShowForm] = useState(false)
  const [saving, setSaving] = useState(false)
  const [receiving, setReceiving] = useState<number | null>(null)
  const [error, setError] = useState('')
  const [supplierQuery, setSupplierQuery] = useState('')
  const [suggestions, setSuggestions] = useState<Partner[]>([])
  const [supplier, setSupplier] = useState<Partner | null>(null)
  const [form, setForm] = useState({ number: '', supplier: '', issue_date: '09/09/2026', due_date: '', payment_method: 'Boleto', description: 'Compra de insumos', barcode: '', category: 'Insumos', notes: '' })
  const [items, setItems] = useState([emptyItem])
  useEffect(() => { apiFetch<PurchaseInvoice[]>('/invoices', token).then(onLoaded).catch((requestError: unknown) => setError(requestError instanceof Error ? requestError.message : 'Erro ao carregar NFs')) }, [token, onLoaded])
  useEffect(() => { if (supplierQuery.trim().length < 2) { setSuggestions([]); return }; const timer = window.setTimeout(() => apiFetch<Partner[]>(`/suppliers?q=${encodeURIComponent(supplierQuery)}`, token).then(setSuggestions).catch(() => setSuggestions([])), 220); return () => window.clearTimeout(timer) }, [supplierQuery, token])
  function updateItem(index: number, field: keyof typeof emptyItem, value: string) { setItems((current) => current.map((item, itemIndex) => itemIndex === index ? { ...item, [field]: value } : item)) }
  async function createInvoice(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setSaving(true); setError(''); try { const payload = { number: form.number, supplier: form.supplier, supplier_id: supplier?.id ?? null, issue_date: form.issue_date, due_date: form.due_date, payment_method: form.payment_method, description: form.description, barcode: form.barcode, category: form.category, notes: form.notes, items: items.map((item) => ({ ...item, quantity: Number(item.quantity), unit_cost: Number(item.unit_cost), description: inventory.find((stockItem) => stockItem.sku === item.sku)?.name ?? item.sku })) }; const invoice = await apiFetch<PurchaseInvoice>('/invoices', token, { method: 'POST', body: JSON.stringify(payload) }); onLoaded([invoice, ...invoices]); setShowForm(false); setItems([emptyItem]); setForm({ number: '', supplier: '', issue_date: '09/09/2026', due_date: '', payment_method: 'Boleto', description: 'Compra de insumos', barcode: '', category: 'Insumos', notes: '' }); setSupplier(null); setSupplierQuery('') } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao cadastrar NF') } finally { setSaving(false) } }
  async function receive(invoice: PurchaseInvoice) { setReceiving(invoice.id); setError(''); try { onReceived(await apiFetch<PurchaseInvoice>(`/invoices/${invoice.id}/receive`, token, { method: 'POST' })) } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao dar entrada') } finally { setReceiving(null) } }
  return <><PageHeading eyebrow="Fiscal · Almoxarifado" title="Notas fiscais de entrada" description="Registre documentos de fornecedores e dê entrada nos materiais com rastreabilidade." action={<button className="primary-button" onClick={() => setShowForm(true)}><Plus size={17} /> Nova NF de entrada</button>} />{error && <div className="api-error"><AlertTriangle size={15} />{error}</div>}{showForm && <section className="panel create-panel"><div className="panel-header"><div><span className="panel-kicker">Recebimento fiscal</span><h2>Registrar nota fiscal</h2></div><button className="icon-button" onClick={() => setShowForm(false)} aria-label="Fechar"><X size={16} /></button></div><form className="create-form invoice-form" onSubmit={createInvoice}><label>Número da NF<input required value={form.number} onChange={(event) => setForm({ ...form, number: event.target.value })} placeholder="02842" /></label><label>Fornecedor<div className="autocomplete"><input required value={form.supplier} onChange={(event) => { setForm({ ...form, supplier: event.target.value }); setSupplierQuery(event.target.value); setSupplier(null) }} placeholder="Digite nome, fantasia ou CNPJ" />{suggestions.length > 0 && <div className="suggestion-list">{suggestions.map((item) => <button type="button" key={item.id} onClick={() => { setForm({ ...form, supplier: item.trade_name }); setSupplier(item); setSupplierQuery(item.trade_name); setSuggestions([]) }}><strong>{item.trade_name}</strong><small>{item.legal_name} · {item.document}</small></button>)}</div>}</div></label><label>Data de emissão<input required value={form.issue_date} onChange={(event) => setForm({ ...form, issue_date: event.target.value })} /></label><div className="invoice-items-field"><div className="invoice-items-heading"><strong>Itens da nota ({items.length})</strong><button type="button" className="secondary-button" onClick={() => setItems([...items, emptyItem])}><Plus size={14} /> Adicionar item</button></div>{items.map((item, index) => <div className="invoice-item-editor" key={index}><select required value={item.sku} onChange={(event) => updateItem(index, 'sku', event.target.value)}>{inventory.map((stockItem) => <option key={stockItem.sku} value={stockItem.sku}>{stockItem.sku} · {stockItem.name}</option>)}</select><input required min="0.01" type="number" step="0.01" value={item.quantity} onChange={(event) => updateItem(index, 'quantity', event.target.value)} placeholder="Quantidade" /><input required min="0.01" type="number" step="0.01" value={item.unit_cost} onChange={(event) => updateItem(index, 'unit_cost', event.target.value)} placeholder="Custo unitário" />{items.length > 1 && <button type="button" className="icon-button" onClick={() => setItems(items.filter((_, itemIndex) => itemIndex !== index))} aria-label="Remover item"><X size={15} /></button>}</div>)}</div><button className="primary-button" disabled={saving}>{saving ? 'Registrando...' : 'Salvar NF pendente'}</button></form></section>}<section className="panel full-panel"><div className="panel-header"><div><span className="panel-kicker">Documentos recentes</span><h2>Entradas fiscais</h2></div><span className="draft-status">{invoices.length} documentos</span></div><div className="invoice-list">{invoices.length === 0 ? <div className="empty-state"><Receipt size={22} /><strong>Nenhuma NF registrada</strong><span>Cadastre a primeira nota para iniciar uma entrada de estoque.</span></div> : invoices.map((invoice) => <div className="invoice-row" key={invoice.id}><div className="invoice-icon"><Receipt size={17} /></div><div className="invoice-main"><strong>NF {invoice.number}</strong><small>{invoice.supplier} · {invoice.issue_date}</small></div><div><strong>{formatBRL(Number(invoice.total))}</strong><small>{invoice.items.length} item(ns)</small></div><span className={`pill-status ${invoice.status === 'Recebida' ? 'concluída' : 'aguardando'}`}>{invoice.status}</span>{invoice.status === 'Pendente' && <button className="secondary-button" disabled={receiving === invoice.id} onClick={() => receive(invoice)}>{receiving === invoice.id ? 'Processando...' : 'Dar entrada'}</button>}</div>)}</div></section></>
}

function PartnerView({ token, kind }: { token: string; kind: 'suppliers' | 'customers' }) {
  const isSupplier = kind === 'suppliers'
  const [partners, setPartners] = useState<Partner[]>([])
  const [searchTerm, setSearchTerm] = useState('')
  const [showForm, setShowForm] = useState(false)
  const [editing, setEditing] = useState<Partner | null>(null)
  const [saving, setSaving] = useState(false)
  const [error, setError] = useState('')
  const emptyForm = { document: '', legal_name: '', trade_name: '', email: '', phone: '', address: '', city: '', state: '', zip_code: '' }
  const [form, setForm] = useState(emptyForm)
  useEffect(() => { apiFetch<Partner[]>(`/${kind}${searchTerm ? `?q=${encodeURIComponent(searchTerm)}` : ''}`, token).then(setPartners).catch((requestError: unknown) => setError(requestError instanceof Error ? requestError.message : 'Erro ao carregar cadastro')) }, [kind, searchTerm, token])
  function openCreate() { setEditing(null); setForm(emptyForm); setError(''); setShowForm(true) }
  function openEdit(partner: Partner) { setEditing(partner); setForm({ document: partner.document, legal_name: partner.legal_name, trade_name: partner.trade_name, email: partner.email, phone: partner.phone, address: partner.address, city: partner.city, state: partner.state, zip_code: partner.zip_code }); setError(''); setShowForm(true) }
  async function savePartner(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setSaving(true); setError(''); try { const path = editing ? `/${kind}/${editing.id}` : `/${kind}`; const saved = await apiFetch<Partner>(path, token, { method: editing ? 'PUT' : 'POST', body: JSON.stringify(form) }); setPartners((current) => editing ? current.map((item) => item.id === saved.id ? saved : item) : [saved, ...current]); setShowForm(false) } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao salvar cadastro') } finally { setSaving(false) } }
  async function removePartner(partner: Partner) { if (!window.confirm(`Excluir ${partner.trade_name || partner.legal_name}?`)) return; try { await apiFetch<unknown>(`/${kind}/${partner.id}`, token, { method: 'DELETE' }); setPartners((current) => current.filter((item) => item.id !== partner.id)) } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao excluir cadastro') } }
  const title = isSupplier ? 'Fornecedores' : 'Clientes'
  return <><PageHeading eyebrow={`Cadastros · ${isSupplier ? 'Compras' : 'Comercial'}`} title={title} description={isSupplier ? 'Mantenha os dados fiscais e de contato dos parceiros que abastecem a fábrica.' : 'Organize os dados dos clientes para vendas e relacionamento.'} action={<button className="primary-button" onClick={openCreate}><Plus size={17} /> Novo {isSupplier ? 'fornecedor' : 'cliente'}</button>} />{error && <div className="api-error"><AlertTriangle size={15} />{error}</div>}{showForm && <section className="panel create-panel"><div className="panel-header"><div><span className="panel-kicker">{editing ? 'Editar cadastro' : 'Novo cadastro'}</span><h2>{editing ? editing.trade_name : `Cadastrar ${isSupplier ? 'fornecedor' : 'cliente'}`}</h2></div><button className="icon-button" onClick={() => setShowForm(false)} aria-label="Fechar"><X size={16} /></button></div><form className="create-form partner-form" onSubmit={savePartner}><label>CNPJ / CPF<input required disabled={Boolean(editing)} value={form.document} onChange={(event) => setForm({ ...form, document: event.target.value })} placeholder="00.000.000/0001-00" /></label><label>Razão social / nome real<input required value={form.legal_name} onChange={(event) => setForm({ ...form, legal_name: event.target.value })} /></label><label>Nome fantasia<input required={isSupplier} value={form.trade_name} onChange={(event) => setForm({ ...form, trade_name: event.target.value })} /></label><label>E-mail<input type="email" value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} /></label><label>Telefone<input required={isSupplier} value={form.phone} onChange={(event) => setForm({ ...form, phone: event.target.value })} placeholder="(41) 99999-9999" /></label><label>CEP<input value={form.zip_code} onChange={(event) => setForm({ ...form, zip_code: event.target.value })} /></label><label>Endereço<input value={form.address} onChange={(event) => setForm({ ...form, address: event.target.value })} /></label><label>Cidade<input value={form.city} onChange={(event) => setForm({ ...form, city: event.target.value })} /></label><label>UF<input maxLength={2} value={form.state} onChange={(event) => setForm({ ...form, state: event.target.value.toUpperCase() })} /></label><button className="primary-button" disabled={saving}>{saving ? 'Salvando...' : editing ? 'Salvar alterações' : 'Cadastrar'}</button></form></section>}<section className="panel full-panel"><div className="panel-toolbar"><div className="search-box"><Search size={16} /><input value={searchTerm} onChange={(event) => setSearchTerm(event.target.value)} placeholder={`Buscar ${isSupplier ? 'fornecedor' : 'cliente'} por nome ou documento`} /></div><span className="draft-status">{partners.length} registros</span></div><div className="partner-grid">{partners.map((partner) => <article className="partner-card" key={partner.id}><div className="partner-avatar">{(partner.trade_name || partner.legal_name).slice(0, 2).toUpperCase()}</div><div className="partner-card-main"><strong>{partner.trade_name || partner.legal_name}</strong><small>{partner.legal_name}</small><span>{partner.document}</span><span>{partner.email || 'Sem e-mail'} · {partner.phone || 'Sem telefone'}</span></div><div className="card-actions"><button className="mini-action" onClick={() => openEdit(partner)}>Editar</button><button className="mini-action danger" onClick={() => removePartner(partner)}>Excluir</button></div></article>)}</div>{partners.length === 0 && <div className="empty-state"><Users size={22} /><strong>Nenhum cadastro encontrado</strong><span>Crie o primeiro registro para começar.</span></div>}</section></>
}

const settingSections = [
  { key: 'general', label: 'Geral', description: 'Identidade e preferências da aplicação.' },
  { key: 'production', label: 'Produção', description: 'Parâmetros do PCP e jornada fabril.' },
  { key: 'inventory', label: 'Estoque', description: 'Regras de saldo e alertas.' },
  { key: 'purchasing', label: 'Compras', description: 'Comportamento das entradas e fornecedores.' },
  { key: 'finance', label: 'Financeiro', description: 'Alertas e padrões de pagamento.' },
  { key: 'fiscal', label: 'Fiscal · SEFAZ', description: 'Preparação da integração fiscal futura.' },
]

function SettingsView({ token }: { token: string }) {
  const [settings, setSettings] = useState<AppSetting[]>([])
  const [active, setActive] = useState('general')
  const [draft, setDraft] = useState<Record<string, string | number | boolean>>({})
  const [saving, setSaving] = useState(false)
  const [message, setMessage] = useState('')
  const [error, setError] = useState('')
  useEffect(() => { apiFetch<AppSetting[]>('/settings', token).then((result) => { setSettings(result); setDraft(result.find((setting) => setting.category === active)?.payload ?? {}) }).catch((requestError: unknown) => setError(requestError instanceof Error ? requestError.message : 'Erro ao carregar configurações')) }, [token])
  function selectSection(category: string) { setActive(category); setMessage(''); setDraft(settings.find((setting) => setting.category === category)?.payload ?? {}) }
  function update(key: string, value: string | number | boolean) { setDraft((current) => ({ ...current, [key]: value })) }
  async function save() { setSaving(true); setMessage(''); setError(''); try { const saved = await apiFetch<AppSetting>(`/settings/${active}`, token, { method: 'PUT', body: JSON.stringify({ payload: draft }) }); setSettings((current) => current.map((setting) => setting.category === active ? saved : setting)); setMessage('Configurações salvas') } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao salvar configurações') } finally { setSaving(false) } }
  const fields = active === 'general' ? [{ key: 'company_name', label: 'Nome da empresa', type: 'text' }, { key: 'timezone', label: 'Fuso horário', type: 'text' }, { key: 'date_format', label: 'Formato de data', type: 'text' }, { key: 'currency', label: 'Moeda', type: 'text' }] : active === 'production' ? [{ key: 'default_shift', label: 'Turno padrão', type: 'text' }, { key: 'working_days', label: 'Dias de trabalho', type: 'text' }, { key: 'allow_backdated_entries', label: 'Permitir apontamentos retroativos', type: 'checkbox' }] : active === 'inventory' ? [{ key: 'negative_stock', label: 'Permitir estoque negativo', type: 'checkbox' }, { key: 'default_movement_reason', label: 'Motivo padrão de movimentação', type: 'text' }, { key: 'low_stock_alert', label: 'Alertas de estoque mínimo', type: 'checkbox' }] : active === 'purchasing' ? [{ key: 'require_supplier_on_invoice', label: 'Exigir fornecedor na NF', type: 'checkbox' }, { key: 'auto_create_payable', label: 'Criar conta a pagar automaticamente', type: 'checkbox' }] : active === 'finance' ? [{ key: 'due_alert_days', label: 'Alertar vencimentos com antecedência de dias', type: 'number' }, { key: 'default_payment_method', label: 'Forma de pagamento padrão', type: 'text' }, { key: 'allow_overdue_payment', label: 'Permitir baixa após vencimento', type: 'checkbox' }] : [{ key: 'provider', label: 'Provedor fiscal', type: 'text' }, { key: 'document_type', label: 'Documento fiscal', type: 'select', options: ['NF-e', 'NFC-e'] }, { key: 'environment', label: 'Ambiente SEFAZ', type: 'select', options: ['homologacao', 'producao'] }, { key: 'state', label: 'UF do emitente', type: 'text' }, { key: 'issuer_cnpj', label: 'CNPJ do emitente', type: 'text' }, { key: 'state_registration', label: 'Inscrição estadual', type: 'text' }, { key: 'series', label: 'Série', type: 'text' }, { key: 'next_number', label: 'Próximo número', type: 'text' }, { key: 'certificate_type', label: 'Tipo de certificado', type: 'select', options: ['A1', 'A3'] }, { key: 'certificate_reference', label: 'Referência do certificado', type: 'text' }, { key: 'certificate_expires_at', label: 'Validade do certificado', type: 'text' }, { key: 'csc_reference', label: 'Referência CSC/token', type: 'text' }, { key: 'technical_contact', label: 'Contato técnico', type: 'text' }]
  const activeSection = settingSections.find((section) => section.key === active)
  return <><PageHeading eyebrow="Sistema · Administração" title="Configurações" description="Preferências gerais e parâmetros de cada módulo do Atelier ERP." action={<button className="primary-button" onClick={save} disabled={saving}><ShieldCheck size={16} />{saving ? 'Salvando...' : 'Salvar alterações'}</button>} />{message && <div className="settings-success"><ShieldCheck size={15} />{message}</div>}{error && <div className="api-error"><AlertTriangle size={15} />{error}</div>}<div className="settings-layout"><aside className="settings-menu">{settingSections.map((section) => <button key={section.key} className={active === section.key ? 'active' : ''} onClick={() => selectSection(section.key)}><strong>{section.label}</strong><small>{section.description}</small></button>)}</aside><section className="panel settings-panel"><div className="panel-header"><div><span className="panel-kicker">{activeSection?.label}</span><h2>Parâmetros do módulo</h2></div>{active === 'fiscal' && <span className="draft-status">Preparação SEFAZ</span>}</div>{active === 'fiscal' && <div className="settings-callout"><ShieldCheck size={16} /><span>Esta tela prepara o contrato de integração com SEFAZ. O certificado e os tokens devem ser referenciados por um cofre de segredos no ambiente de produção; nenhum documento será transmitido por este MVP.</span></div>}<div className="settings-form">{fields.map((field) => <label className={field.type === 'checkbox' ? 'setting-toggle' : ''} key={field.key}>{field.type === 'checkbox' ? <><input type="checkbox" checked={Boolean(draft[field.key])} onChange={(event) => update(field.key, event.target.checked)} /><span>{field.label}</span></> : <>{field.label}{field.type === 'select' ? <select value={String(draft[field.key] ?? '')} onChange={(event) => update(field.key, event.target.value)}>{field.options?.map((option) => <option key={option}>{option}</option>)}</select> : <input type={field.type} value={String(draft[field.key] ?? '')} onChange={(event) => update(field.key, field.type === 'number' ? Number(event.target.value) : event.target.value)} />}</>}</label>)}</div></section></div></>
}

const permissionOptions = [
  { key: 'dashboard', label: 'Visão geral' },
  { key: 'production', label: 'Produção' },
  { key: 'inventory', label: 'Estoque' },
  { key: 'movements', label: 'Movimentações' },
  { key: 'purchasing', label: 'Notas fiscais e compras' },
  { key: 'finance', label: 'Financeiro' },
  { key: 'partners', label: 'Fornecedores e clientes' },
  { key: 'settings', label: 'Configurações' },
]

function UsersView({ token }: { token: string }) {
  const [users, setUsers] = useState<AppUser[]>([])
  const [showForm, setShowForm] = useState(false)
  const [editing, setEditing] = useState<AppUser | null>(null)
  const [error, setError] = useState('')
  const [saving, setSaving] = useState(false)
  const [form, setForm] = useState({ name: '', email: '', password: '', permissions: [] as string[] })
  useEffect(() => { apiFetch<AppUser[]>('/users', token).then(setUsers).catch((requestError: unknown) => setError(requestError instanceof Error ? requestError.message : 'Erro ao carregar usuários')) }, [token])
  function startCreate() { setEditing(null); setForm({ name: '', email: '', password: '', permissions: [] }); setError(''); setShowForm(true) }
  function startEdit(user: AppUser) { setEditing(user); setForm({ name: user.name, email: user.email, password: '', permissions: user.permissions }); setError(''); setShowForm(true) }
  function togglePermission(permission: string) { setForm((current) => ({ ...current, permissions: current.permissions.includes(permission) ? current.permissions.filter((item) => item !== permission) : [...current.permissions, permission] })) }
  async function saveUser(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setSaving(true); setError(''); try { const path = editing ? `/users/${editing.id}` : '/users'; const payload = editing && !form.password ? { name: form.name, email: form.email, permissions: form.permissions, is_active: editing.permissions !== undefined } : form; const saved = await apiFetch<AppUser>(path, token, { method: editing ? 'PUT' : 'POST', body: JSON.stringify(payload) }); setUsers((current) => editing ? current.map((item) => item.id === saved.id ? saved : item) : [...current, saved]); setShowForm(false) } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao salvar usuário') } finally { setSaving(false) } }
  async function deactivate(user: AppUser) { if (!window.confirm(`Desativar ${user.name}?`)) return; try { await apiFetch<unknown>(`/users/${user.id}`, token, { method: 'DELETE' }); setUsers((current) => current.filter((item) => item.id !== user.id)) } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao desativar usuário') } }
  return <><PageHeading eyebrow="Administração · Acesso" title="Usuários" description="Crie acessos e defina quais módulos cada pessoa pode visualizar." action={<button className="primary-button" onClick={startCreate}><Plus size={17} /> Novo usuário</button>} />{error && <div className="api-error"><AlertTriangle size={15} />{error}</div>}{showForm && <section className="panel create-panel"><div className="panel-header"><div><span className="panel-kicker">{editing ? 'Editar acesso' : 'Novo acesso'}</span><h2>{editing ? editing.name : 'Criar usuário'}</h2></div><button className="icon-button" onClick={() => setShowForm(false)} aria-label="Fechar"><X size={16} /></button></div><form className="create-form user-form" onSubmit={saveUser}><label>Nome<input required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} /></label><label>E-mail<input required type="email" value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} /></label><label>Senha<input required={!editing} type="password" value={form.password} onChange={(event) => setForm({ ...form, password: event.target.value })} placeholder={editing ? 'Deixe em branco para manter' : 'Mínimo de 6 caracteres'} /></label><div className="permission-field"><strong>Permissões por módulo</strong><small>Defina o que este usuário poderá visualizar.</small><div className="permission-grid">{permissionOptions.map((permission) => <label className="setting-toggle" key={permission.key}><input type="checkbox" checked={form.permissions.includes(permission.key)} onChange={() => togglePermission(permission.key)} /><span>{permission.label}</span></label>)}</div></div><button className="primary-button" disabled={saving}>{saving ? 'Salvando...' : editing ? 'Salvar alterações' : 'Criar usuário'}</button></form></section>}<section className="panel full-panel"><div className="panel-header"><div><span className="panel-kicker">Controle de acesso</span><h2>Usuários cadastrados</h2></div><span className="draft-status">{users.length} registros</span></div><div className="user-list">{users.map((user) => <div className="user-row" key={user.id}><div className="user-avatar">{user.name.slice(0, 2).toUpperCase()}</div><div><strong>{user.name}</strong><small>{user.email} · {user.role}</small></div><div className="user-permissions">{user.permissions.length ? `${user.permissions.length} módulos` : 'Sem permissões específicas'}</div><div className="card-actions"><button className="mini-action" onClick={() => startEdit(user)}>Editar</button><button className="mini-action danger" onClick={() => deactivate(user)}>Desativar</button></div></div>)}</div></section></>
}

function MovementsView({ token, inventory, onChanged }: { token: string; inventory: InventoryItem[]; onChanged: () => void }) {
  const [movements, setMovements] = useState<StockMovement[]>([])
  const [showForm, setShowForm] = useState(false)
  const [saving, setSaving] = useState(false)
  const [error, setError] = useState('')
  const [form, setForm] = useState({ inventory_item_id: String(inventory[0]?.id ?? ''), movement_type: 'Entrada' as StockMovement['movement_type'], quantity: '1', reason: '', reference: '', movement_date: '09/09/2026' })
  useEffect(() => { apiFetch<StockMovement[]>('/stock-movements', token).then(setMovements).catch((requestError: unknown) => setError(requestError instanceof Error ? requestError.message : 'Erro ao carregar movimentações')) }, [token])
  async function createMovement(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setSaving(true); setError(''); try { const movement = await apiFetch<StockMovement>('/stock-movements', token, { method: 'POST', body: JSON.stringify({ ...form, inventory_item_id: Number(form.inventory_item_id), quantity: Number(form.quantity) }) }); setMovements((current) => [movement, ...current]); onChanged(); setShowForm(false); setForm({ ...form, quantity: '1', reason: '', reference: '' }) } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao registrar movimentação') } finally { setSaving(false) } }
  return <><PageHeading eyebrow="Estoque · Rastreabilidade" title="Movimentações" description="Registre entradas e saídas e acompanhe o histórico de cada material." action={<button className="primary-button" onClick={() => setShowForm(true)}><Plus size={17} /> Nova movimentação</button>} />{error && <div className="api-error"><AlertTriangle size={15} />{error}</div>}<div className="metric-grid"><MetricCard label="Movimentações" value={String(movements.length)} detail="histórico carregado" trend="API" direction="up" accent="teal" /><MetricCard label="Entradas" value={String(movements.filter((item) => item.movement_type === 'Entrada').length)} detail="reposição e recebimento" trend="API" direction="up" accent="cream" /><MetricCard label="Saídas" value={String(movements.filter((item) => item.movement_type === 'Saída').length)} detail="consumo e ajustes" trend="API" direction="down" accent="peach" /><MetricCard label="Materiais" value={String(inventory.length)} detail="itens disponíveis" trend="API" direction="up" accent="lilac" /></div>{showForm && <section className="panel create-panel"><div className="panel-header"><div><span className="panel-kicker">Estoque</span><h2>Registrar movimentação</h2></div><button className="icon-button" onClick={() => setShowForm(false)} aria-label="Fechar"><X size={16} /></button></div><form className="create-form" onSubmit={createMovement}><label>Material<select required value={form.inventory_item_id} onChange={(event) => setForm({ ...form, inventory_item_id: event.target.value })}>{inventory.map((item) => <option key={item.id} value={item.id}>{item.sku} · {item.name} · saldo {item.stock} {item.unit}</option>)}</select></label><label>Tipo<select value={form.movement_type} onChange={(event) => setForm({ ...form, movement_type: event.target.value as StockMovement['movement_type'] })}><option>Entrada</option><option>Saída</option></select></label><label>Quantidade<input required min="0.01" type="number" step="0.01" value={form.quantity} onChange={(event) => setForm({ ...form, quantity: event.target.value })} /></label><label>Motivo<input required value={form.reason} onChange={(event) => setForm({ ...form, reason: event.target.value })} placeholder="Consumo na OP, ajuste..." /></label><label>Referência<input value={form.reference} onChange={(event) => setForm({ ...form, reference: event.target.value })} placeholder="OP-2409 ou NF 02842" /></label><label>Data<input required value={form.movement_date} onChange={(event) => setForm({ ...form, movement_date: event.target.value })} /></label><button className="primary-button" disabled={saving}>{saving ? 'Registrando...' : 'Registrar movimentação'}</button></form></section>}<section className="panel full-panel"><div className="panel-header"><div><span className="panel-kicker">Livro de estoque</span><h2>Histórico de movimentações</h2></div><span className="draft-status">{movements.length} registros</span></div><div className="movement-list">{movements.length === 0 ? <div className="empty-state"><PackageCheck size={22} /><strong>Nenhuma movimentação registrada</strong><span>Registre a primeira entrada ou saída manual.</span></div> : movements.map((movement) => <div className="movement-row" key={movement.id}><div className={`movement-icon ${movement.movement_type === 'Entrada' ? 'in' : 'out'}`}>{movement.movement_type === 'Entrada' ? <ArrowUpRight size={17} /> : <ArrowDownRight size={17} />}</div><div><strong>{movement.item_name}</strong><small>{movement.sku} · {movement.reason}</small></div><div><strong className={movement.movement_type === 'Entrada' ? 'movement-in' : 'movement-out'}>{movement.movement_type === 'Entrada' ? '+' : '-'}{movement.quantity.toLocaleString('pt-BR')}</strong><small>{movement.movement_date}</small></div><span>{movement.reference || 'Sem referência'}</span></div>)}</div></section></>
}

function PayablesView({ token }: { token: string }) {
  const [accounts, setAccounts] = useState<Array<{ id: number; supplier_name: string; due_date: string; purchase_date: string; amount: number; payment_method: string; description: string; barcode: string; category: string; status: string }>>([])
  const [showForm, setShowForm] = useState(false)
  const [error, setError] = useState('')
  const [form, setForm] = useState({ supplier_name: '', due_date: '', purchase_date: '09/09/2026', amount: '', payment_method: 'Boleto', description: '', barcode: '', category: 'Insumos', notes: '' })
  useEffect(() => { apiFetch<typeof accounts>('/finance/payables', token).then(setAccounts).catch((requestError: unknown) => setError(requestError instanceof Error ? requestError.message : 'Erro ao carregar contas')) }, [token])
  async function createAccount(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setError(''); try { const account = await apiFetch<typeof accounts[number]>('/finance/payables', token, { method: 'POST', body: JSON.stringify({ ...form, amount: Number(form.amount) }) }); setAccounts((current) => [account, ...current]); setShowForm(false); setForm({ ...form, supplier_name: '', amount: '', description: '', barcode: '' }) } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao salvar conta') } }
  async function pay(account: typeof accounts[number]) { try { const updated = await apiFetch<typeof account>(`/finance/payables/${account.id}/pay`, token, { method: 'POST' }); setAccounts((current) => current.map((item) => item.id === updated.id ? updated : item)) } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao baixar conta') } }
  const openAmount = accounts.filter((account) => account.status !== 'Paga').reduce((total, account) => total + Number(account.amount), 0)
  return <><PageHeading eyebrow="Financeiro · Obrigações" title="Contas a pagar" description="Registre despesas, acompanhe vencimentos e marque pagamentos realizados." action={<button className="primary-button" onClick={() => setShowForm(true)}><Plus size={17} /> Nova conta</button>} />{error && <div className="api-error"><AlertTriangle size={15} />{error}</div>}<div className="metric-grid"><MetricCard label="Em aberto" value={formatBRL(openAmount)} detail="total pendente" trend="API" direction="down" accent="peach" /><MetricCard label="Contas cadastradas" value={String(accounts.length)} detail="todas as despesas" trend="API" direction="up" accent="teal" /><MetricCard label="Próximo vencimento" value={accounts[0]?.due_date ?? 'Nenhum'} detail="ordenação por data" trend="" direction="up" accent="cream" /><MetricCard label="Pagas" value={String(accounts.filter((account) => account.status === 'Paga').length)} detail="baixas realizadas" trend="API" direction="up" accent="lilac" /></div>{showForm && <section className="panel create-panel"><div className="panel-header"><div><span className="panel-kicker">Nova obrigação</span><h2>Adicionar conta a pagar</h2></div><button className="icon-button" onClick={() => setShowForm(false)} aria-label="Fechar"><X size={16} /></button></div><form className="create-form finance-form" onSubmit={createAccount}><label>Fornecedor<input value={form.supplier_name} onChange={(event) => setForm({ ...form, supplier_name: event.target.value })} placeholder="Nome do fornecedor" /></label><label>Descrição<input required value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} placeholder="Compra de matéria-prima" /></label><label>Valor<input required min="0.01" type="number" step="0.01" value={form.amount} onChange={(event) => setForm({ ...form, amount: event.target.value })} /></label><label>Data da compra<input required value={form.purchase_date} onChange={(event) => setForm({ ...form, purchase_date: event.target.value })} placeholder="09/09/2026" /></label><label>Data de vencimento<input required value={form.due_date} onChange={(event) => setForm({ ...form, due_date: event.target.value })} placeholder="09/10/2026" /></label><label>Forma de pagamento<select value={form.payment_method} onChange={(event) => setForm({ ...form, payment_method: event.target.value })}><option>Boleto</option><option>PIX</option><option>Transferência</option><option>Cartão</option><option>Dinheiro</option></select></label><label>Categoria<select value={form.category} onChange={(event) => setForm({ ...form, category: event.target.value })}><option>Insumos</option><option>Fretes</option><option>Serviços</option><option>Impostos</option><option>Outros</option></select></label><label>Código de barras <span className="field-hint">opcional</span><input value={form.barcode} onChange={(event) => setForm({ ...form, barcode: event.target.value })} /></label><label>Observações<input value={form.notes} onChange={(event) => setForm({ ...form, notes: event.target.value })} /></label><button className="primary-button">Salvar conta</button></form></section>}<section className="panel full-panel"><div className="panel-header"><div><span className="panel-kicker">Agenda financeira</span><h2>Despesas cadastradas</h2></div><span className="draft-status">{accounts.length} contas</span></div><div className="payable-list">{accounts.length === 0 ? <div className="empty-state"><CircleDollarSign size={22} /><strong>Nenhuma conta cadastrada</strong><span>Adicione uma despesa para começar o controle financeiro.</span></div> : accounts.map((account) => <div className="payable-row" key={account.id}><div className="payable-icon"><CircleDollarSign size={17} /></div><div><strong>{account.description}</strong><small>{account.supplier_name || 'Sem fornecedor'} · {account.category}</small></div><div><strong>{formatBRL(Number(account.amount))}</strong><small>Vence {account.due_date}</small></div><span className={`pill-status ${account.status === 'Paga' ? 'concluída' : 'aguardando'}`}>{account.status}</span>{account.status !== 'Paga' && <button className="secondary-button" onClick={() => pay(account)}>Marcar paga</button>}</div>)}</div></section></>
}

function StockViewLive({ token, inventory, onCreated }: { token: string; inventory: InventoryItem[]; onCreated: (item: InventoryItem) => void }) {
  const [showForm, setShowForm] = useState(false)
  const [saving, setSaving] = useState(false)
  const [error, setError] = useState('')
  const [form, setForm] = useState({ sku: '', name: '', stock: '0', unit: 'un', minimum_stock: '0' })
  const belowMinimum = inventory.filter((item) => item.stock <= item.minimum_stock).length

  async function createItem(event: React.FormEvent<HTMLFormElement>) {
    event.preventDefault(); setSaving(true); setError('')
    try {
      const item = await apiFetch<InventoryItem>('/inventory', token, { method: 'POST', body: JSON.stringify({ ...form, stock: Number(form.stock), minimum_stock: Number(form.minimum_stock) }) })
      onCreated(item); setForm({ sku: '', name: '', stock: '0', unit: 'un', minimum_stock: '0' }); setShowForm(false)
    } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao cadastrar material') } finally { setSaving(false) }
  }

  return <><PageHeading eyebrow="Almoxarifado · Inventário" title="Estoque e materiais" description="Visibilidade dos insumos críticos, entradas recentes e pontos de reposição." action={<button className="primary-button" onClick={() => setShowForm(true)}><Plus size={17} /> Cadastrar material</button>} /><div className="metric-grid stock-metrics"><MetricCard label="Itens cadastrados" value={String(inventory.length)} detail="dados sincronizados" trend="agora" direction="up" accent="teal" /><MetricCard label="Abaixo do mínimo" value={String(belowMinimum).padStart(2, '0')} detail="reposição necessária" trend={String(belowMinimum)} direction="down" accent="peach" /><MetricCard label="Entradas recentes" value="Hoje" detail="última sincronização" trend="API" direction="up" accent="cream" /><MetricCard label="Giro médio" value="3,8x" detail="últimos 90 dias" trend="0,4x" direction="up" accent="lilac" /></div>{showForm && <section className="panel create-panel"><div className="panel-header"><div><span className="panel-kicker">Novo material</span><h2>Cadastrar item de estoque</h2></div><button className="icon-button" onClick={() => setShowForm(false)} aria-label="Fechar"><X size={16} /></button></div><form className="create-form" onSubmit={createItem}><label>SKU<input required value={form.sku} onChange={(event) => setForm({ ...form, sku: event.target.value })} placeholder="MAT-040" /></label><label>Descrição<input required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} placeholder="Cola PVA extra forte" /></label><label>Quantidade<input required min="0" type="number" step="0.01" value={form.stock} onChange={(event) => setForm({ ...form, stock: event.target.value })} /></label><label>Unidade<select value={form.unit} onChange={(event) => setForm({ ...form, unit: event.target.value })}><option>un</option><option>m</option><option>m³</option><option>L</option><option>kg</option></select></label><label>Estoque mínimo<input required min="0" type="number" step="0.01" value={form.minimum_stock} onChange={(event) => setForm({ ...form, minimum_stock: event.target.value })} /></label>{error && <div className="login-error"><AlertTriangle size={15} />{error}</div>}<button className="primary-button" disabled={saving}>{saving ? 'Salvando...' : 'Salvar material'}</button></form></section>}<section className="panel full-panel"><div className="panel-header"><div><span className="panel-kicker">Inventário atual</span><h2>Materiais sincronizados</h2></div><button className="text-button">Dados reais <ShieldCheck size={15} /></button></div><div className="inventory-grid">{inventory.map((item) => <div className="inventory-card" key={item.sku}><div className={`inventory-card-icon ${item.color}`}><Boxes size={19} /></div><div className="inventory-card-top"><span>{item.sku}</span><span className={`inventory-state ${item.color}`}>{item.state}</span></div><h3>{item.name}</h3><strong>{item.stock.toLocaleString('pt-BR')} {item.unit}</strong><div className="inventory-meter"><span className={item.color} style={{ width: `${inventoryLevel(item)}%` }} /></div><div className="inventory-footer"><small>Última entrada: {item.last_entry}</small><button className="icon-button"><ChevronRight size={15} /></button></div></div>)}</div></section></>
}

function PricingViewLive({ token, cost, setCost, margin, setMargin, tax, setTax, salePrice, contribution }: { token: string; cost: number; setCost: (value: number) => void; margin: number; setMargin: (value: number) => void; tax: number; setTax: (value: number) => void; salePrice: number; contribution: number }) {
  const [result, setResult] = useState<{ suggested_price: number; contribution: number; estimated_tax: number; markup: number; fiscal_status: string } | null>(null)
  const [saving, setSaving] = useState(false)
  const [error, setError] = useState('')
  const displayedPrice = result?.suggested_price ?? salePrice
  const displayedContribution = result?.contribution ?? contribution

  async function simulate() {
    setSaving(true); setError('')
    try {
      const response = await apiFetch<{ suggested_price: string | number; contribution: string | number; estimated_tax: string | number; markup: string | number; fiscal_status: string }>('/pricing/simulate', token, { method: 'POST', body: JSON.stringify({ cost, tax, margin }) })
      setResult({ suggested_price: Number(response.suggested_price), contribution: Number(response.contribution), estimated_tax: Number(response.estimated_tax), markup: Number(response.markup), fiscal_status: response.fiscal_status })
    } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Erro ao simular preço') } finally { setSaving(false) }
  }

  return <><PageHeading eyebrow="Custos industriais · Simulação" title="Custos e preços" description="Modele o preço de venda com base no custo fabril e nos parâmetros comerciais." action={<button className="secondary-button" onClick={simulate}><Calculator size={16} /> {saving ? 'Calculando...' : 'Calcular na API'}</button>} /><div className="pricing-layout"><section className="panel pricing-inputs"><div className="panel-header"><div><span className="panel-kicker">Simulador de preço</span><h2>Parâmetros da Cadeira Lina</h2></div><span className="draft-status">API</span></div><div className="input-group"><label>Custo unitário de fabricação <span>CUF</span></label><div className="money-input"><span>R$</span><input type="number" value={cost} onChange={(event) => setCost(Number(event.target.value))} /></div><small>MP + MOD + CIF rateado</small></div><div className="input-group"><label>Tributos sobre venda <span>Regime pendente</span></label><div className="range-value"><input type="range" min="0" max="30" step="0.05" value={tax} onChange={(event) => setTax(Number(event.target.value))} /><strong>{tax.toFixed(2).replace('.', ',')}%</strong></div><small>Parâmetro provisório para simulação. Valide o regime fiscal.</small></div><div className="input-group"><label>Margem líquida pretendida</label><div className="range-value"><input type="range" min="5" max="60" step="1" value={margin} onChange={(event) => setMargin(Number(event.target.value))} /><strong>{margin}%</strong></div><small>Margem após tributos e despesas comerciais</small></div><div className="cost-breakdown"><div><span>Matéria-prima</span><strong>R$ 74,20</strong></div><div><span>Mão de obra direta</span><strong>R$ 42,80</strong></div><div><span>CIF rateado</span><strong>R$ 31,50</strong></div><div className="breakdown-total"><span>Custo de fabricação</span><strong>{formatBRL(cost)}</strong></div></div>{error && <div className="login-error pricing-error"><AlertTriangle size={15} />{error}</div>}</section><section className="price-result"><div className="result-orbit"><Calculator size={22} /></div><span className="panel-kicker">Preço sugerido</span><strong>{formatBRL(displayedPrice)}</strong><span className="result-note">por unidade · markup divisor</span><div className="result-divider" /><div className="result-stats"><div><span>Margem líquida</span><strong>{formatBRL(displayedContribution)}</strong></div><div><span>Markup aplicado</span><strong>{(result?.markup ?? salePrice / cost).toFixed(2).replace('.', ',')}x</strong></div><div><span>Tributos estimados</span><strong>{formatBRL(result?.estimated_tax ?? salePrice * tax / 100)}</strong></div></div><button className="result-button" onClick={() => alert('Simulação marcada para validação do financeiro.')}><ShieldCheck size={16} /> Validar com financeiro</button></section></div><div className="pricing-note"><AlertTriangle size={16} /><span><strong>Atenção:</strong> {result?.fiscal_status ?? 'o motor fiscal está aguardando a confirmação entre Lucro Real e Simples Nacional. Calcule na API para registrar a simulação.'}</span></div></>
}

export default App

const rootHolder = globalThis as typeof globalThis & { __atelierRoot?: ReturnType<typeof createRoot> }
const root = rootHolder.__atelierRoot ?? createRoot(document.getElementById('root')!)
rootHolder.__atelierRoot = root
root.render(<App />)
